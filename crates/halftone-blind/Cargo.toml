[package]
name = "halftone-blind"
description = "Layer 3: blind forensic classifiers via ONNX Runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true

[dependencies]
halftone-core.workspace = true
serde.workspace = true
serde_json.workspace = true
tracing.workspace = true
# Enable when FeatureProbe::assess is wired (TODO in src/image.rs):
# halftone-packs.workspace = true   # signed model pack + calibration.json
# image.workspace = true            # decode + resize to the encoder input
# ndarray.workspace = true          # input tensor for ort
# ort.workspace = true              # encoder and head ONNX graphs

[lints]
workspace = true
